#pragma once

namespace storm {
    namespace solver {
        
        enum class EquationSystemType {
            UntilProbabilities,
            ReachabilityRewards,
            StochasticShortestPath
        };
        
    }
}
